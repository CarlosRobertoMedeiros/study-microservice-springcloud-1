CREATE TABLE TB_CAMBIO (
  ID int8 NOT NULL GENERATED BY DEFAULT AS IDENTITY,
  NM_TO_CURRENCY CHAR(3) NOT NULL,
  NM_FROM_CURRENCY CHAR(3) NOT NULL,
  VL_CONVERSION_FACTORY numeric(65,2) NOT NULL,
  CONSTRAINT cambio_pkey PRIMARY KEY (id)
);